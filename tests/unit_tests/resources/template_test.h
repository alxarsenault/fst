#pragma once

#include "fst/stream.h"

#define BINGO 33

enum class jesus 
{
    a,
    b,
    c
};

@macro john(33, 56)

@enum bongo

@code(banana, jesus, BINGO, a, b)

print(params[2])

enum_value = fst.create_enum(params[0])

jjjjj = fst.parse_enum("jesus", fst.content);

output = """#include "fst/common.h"

{enum}

int kjkl = {jj};

{list}

@marker {value}
""".format(enum = enum_value, jj = params[2], list = jjjjj, value = 90)

return output;
@endcode

@doc fingo
inline void bingo_bango()
{
    @import data2
    @import data
    fst::print("DJJkDJJDJDJJDJDJDJJDsscddddssdJDD....");
    @import data2
}

@code
return "// DSJKJDKS";
@endcode